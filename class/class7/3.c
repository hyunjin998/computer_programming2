/* 다음과 같이 총 3개의 배열을 선언한다.

char str1[20];
char str2[20];
char str3[40];

그리고는 str1과 str2를 통해서 프로그램 사용자로부터 문자열을 입력 받되,
fgets 함수를 통해서 입력 받는다.
이어서 str1에 저장된 문자열을 str3에 복사하고, 
str2에 저장된 문자열을 str3에 저장된 문자열의 뒤에 덧붙이자.
마지막으로 str3에 저장된 문자열을 출력하자.

[주의!!] str1과 str2에 입력되는 개행문자(\n)는 포함되지 않도록 잘 처리하자.
*/